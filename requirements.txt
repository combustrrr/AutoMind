# AutoMind - Car Recommendation System Requirements

# Core dependencies (Python standard library - no install needed)
# Python >= 3.6 is recommended for f-string support and type hints

# Optional UI dependency
# For Streamlit UI (run: pip install streamlit)
streamlit>=1.20.0  # Optional - for web UI (automind_ui.py)

# ML dependencies (Optional - for ML-powered predictions)
# For ML-based car prediction (train_ml_model.py, ml_guessing_engine.py)
scikit-learn>=1.0.0  # Optional - for ML predictions

# Dataset integration (Optional - for Kaggle dataset download)
kagglehub>=0.2.0  # Optional - for downloading Kaggle datasets
pandas>=1.3.0  # Optional - for dataset processing

# The CLI version (automind_cli.py) requires no external dependencies